/**
 * ---------------------------------------------------------
 * CSV Parser Utility
 * ---------------------------------------------------------
 * Reads CSV files,
 * parses rows,
 * and converts them into usable data.
 */